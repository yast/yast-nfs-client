nfs =
  element nfs {
    LIST,
    element nfs_entry {
      element server_path { text }
    & element mount_point { text }
    & element vfstype {text}?
    & element nfs_options { text }
    }*
  }
